---
globs: []
alwaysApply: true
---

# Output Rules (Cost Saving)

- 回复只输出必须内容，优先直接给代码/补丁
- 禁止写说明文档/README/使用说明
- 禁止写测试代码（除非用户明确要求）
- 每个函数需要使用jsdoc注释（函数的作用，参数的作用），其他不必要注释（除非用户明确要求）
- 不给多个方案让用户选择，直接给最优解
- 只改用户明确要求的范围，避免额外重构/额外功能

